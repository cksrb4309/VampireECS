# Start Here For Codex

아래 요청문을 그대로 보내면 됩니다.

```text
프로젝트 루트의 `unity-agent-kit` 폴더를 기준으로 이 Unity 프로젝트에 에이전트 운영 구조를 설치해줘.

`unity-agent-kit/FOR_CODEX.md`를 읽고, 안내하는 순서대로 단계별로 진행해줘.
```

## 기존 하네스가 이미 있는 경우

```text
프로젝트 루트의 `unity-agent-kit` 폴더를 기준으로 에이전트 운영 구조를 업데이트해줘.

`unity-agent-kit/FOR_CODEX.md`를 읽고, 업데이트 절차를 따라줘.
```
